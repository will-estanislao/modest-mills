/**
 * 24/08/23
 */

const body = document.body;
const leftButton = body.getElementById("left-btn");
const rightButton = body.getElementById("right-btn");

/**
 * Functions
 */

// Moves the images in the carousel
// Determine the button clicked
// Depending on which button is clicked move
function moveImageCarousel(elementId) {
  if (elementId == "left-btn") {
    // Move carousel to left
  } else if (elementId == "right-btn") {
    // Move carousel to right
  }
}

/**
 * Assign Events
 */
